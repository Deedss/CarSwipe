<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_height="wrap_content"
    android:layout_width="match_parent"
    android:id="@+id/chatmessage_row"
    android:orientation="vertical">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="10dp"
        android:paddingEnd="15dp"
        android:paddingStart="60dp"
        android:clipToPadding="false"
        android:id="@+id/msg_sent_body">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/msg_sent"
            android:background="@drawable/sent_message"
            android:textColor="#fff"
            android:padding="10dp"
            android:elevation="2dp"
            android:textSize="18sp"
            android:layout_alignParentEnd="true"
            android:text="@string/placeholder_message"/>

    </RelativeLayout>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="10dp"
        android:paddingStart="15dp"
        android:paddingEnd="60dp"
        android:clipToPadding="false"
        android:id="@+id/msg_recv_body">


        <TextView
            android:id="@+id/name"
            android:layout_marginStart="15dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="4dp"
            android:text="@string/placeholder_sender"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/msg_recv"
            android:layout_below="@+id/name"
            android:layout_alignStart="@+id/name"
            android:background="@drawable/received_message"
            android:paddingVertical="12dp"
            android:paddingHorizontal="16dp"
            android:elevation="2dp"
            android:textSize="18sp"
            android:text="@string/placeholder_message"/>
    </RelativeLayout>
</LinearLayout>

